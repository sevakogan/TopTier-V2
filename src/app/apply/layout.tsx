export const metadata = {
  title: "Apply — Top Tier Miami Club",
  description: "Request an invite to Miami's automotive community.",
};

export default function ApplyLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
